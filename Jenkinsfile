pipeline {
    agent any  // Esto indica que el pipeline puede ejecutarse en cualquier agente disponible

    stages {
        stage('Inicio') {
            steps {
                echo 'Nuevo cambio'  // Este comando imprime "Nuevo cambio" en la consola
            }
        }
    }
}
